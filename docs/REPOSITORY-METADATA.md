# Repository metadata

Recommended GitHub **About** panel values for NeoBrutal Rivet.

## Description

Fluid neo-brutalist design system for humans and LLMs — semantic themes, clamp() scaling, motion, reusable React primitives, and a 61-contract agent-readable registry.

## Website

Target canonical site: `https://neobrutalrivet.github.io/`

Do not add the website to the GitHub About panel until the URL is actually live.

## Hosting model

Use a GitHub organization named `NeoBrutalRivet` with a Pages repository named `neobrutalrivet.github.io`. Keep the design-system source in a separate `NeoBrutal-Rivet` repository under that organization.

## Suggested topics

- `neobrutalism`
- `neo-brutalism`
- `design-system`
- `react`
- `tailwindcss`
- `ui-components`
- `component-library`
- `llm`
- `ai-agents`
- `dark-mode`
